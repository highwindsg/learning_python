#!/usr/bin/env python3

hours = int(input("Enter hours worked: "))
rates = float(input("Enter current rates per hours: "))
Total_Pay = str(hours * rates)
print("Total salary payment", "$ "+Total_Pay)


