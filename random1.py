import random

for i in range(10):         # This range only covers 0 to 9, but not 10.
    x = random.random()     # From 'random', get the random() function.
    print(x)

# The function 'random' returns a random float between 0.0 and 1.0 (including
# 0.0 but not 1.0)
