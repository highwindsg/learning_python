#!/usr/bin/env python3

# Create a dict 'counts' with keys and values.
counts = {
    "chuck": 1,
    "fred": 42,
    "jen": 100
}

# forloop to print out the key, and the value.
for key in counts:
    print(key, counts[key])
